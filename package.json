{
  "name": "mailmeteor-blocks-hub-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Blocks registry for the Chrome extension sidebar.",
  "scripts": {
    "validate": "node scripts/build-registry.mjs --dry-run",
    "build": "node scripts/build-registry.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
